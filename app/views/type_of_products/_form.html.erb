<%= form_for [@service, @type_of_product] do |f| %>
  <% if @type_of_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_of_product.errors.count, "error") %> prohibited this type_of_product from being saved:</h2>

      <ul>
      <% @type_of_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture_url %><br>
    <%= f.text_field :picture_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
